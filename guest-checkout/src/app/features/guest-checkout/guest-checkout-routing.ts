import { Routes } from '@angular/router';
import { GuestCheckoutComponent } from './guest-checkout.component';

export const GUEST_CHECKOUT_ROUTES: Routes = [
  {
    path: '',
    component: GuestCheckoutComponent
  }
]; 